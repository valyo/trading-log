services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Bind mount: DB lives in your project folder and survives Rancher/Docker resets
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
